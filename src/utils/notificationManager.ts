// Re-export notification manager from the component
// This is to avoid React refresh issues with non-component exports
export { notificationManager } from '../components/Notification';
export { useNotifications } from '../components/Notification';